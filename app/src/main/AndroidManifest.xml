<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.aliceprototype">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ForumActivity" />
        <activity android:name=".ProgramActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".Login" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".Dashboard" />
        <activity android:name=".ExamplePrototype" />
        <activity android:name=".UserDash" />
        <activity android:name=".Forum" />
        <activity android:name=".App" />
        <activity android:name=".ForumSlider.MainForum" />
        <activity
            android:name=".LoginTest"
            android:label="@string/title_activity_login_test"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>